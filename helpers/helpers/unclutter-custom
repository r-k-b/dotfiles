#!/usr/bin/env nu
# "hide idle mouse"

if (which xbanish | is-empty) {
    unclutter --timeout 3
} else {
    xbanish -i all -t 3 -m ne # like unclutter, but also prevents unwanted hover states in apps
}

