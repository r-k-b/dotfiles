#!/usr/bin/env nu
# "hide idle mouse"

if (which xbanish | is-empty) {
    unclutter --timeout 6
} else {
    xbanish -i all -t 6 -m ne # like unclutter, but also prevents unwanted hover states in apps
}

